// 01 - object

let cat = {
    name:'garfield',
    age: 3,
    isCute: true,
};
console.log(cat);
console.log(cat['age']);
if (cat.isCute) {
    console.log('so cute !')
}

// 02 - combine

let cat2 = {
    name:'exel',
    age:2,
    isCute: true,
};

let cats = (cat,cat2);

console.log

// 03 - even 

function checkIfEven(num) {

}
